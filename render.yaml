services:
  - type: web
    name: utsavam-backend
    runtime: python
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: utsavam-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: utsavam-redis
          property: connectionString
      - key: ADMIN_KEY
        generateValue: true
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT

  - type: redis
    name: utsavam-redis
    plan: free
    ipAllowList: []

databases:
  - name: utsavam-postgres
    plan: free
    databaseName: utsavam_dev
    ipAllowList: []


